<!DOCTYPE html>
<html>
<head>
  <title>WeatherApp</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Incluir Bootstrap y Font Awesome desde CDN -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  
  <!-- Incluir tus estilos personalizados -->
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  <style>
    body {
      background-color: #101820;
      font-family: 'Arial', sans-serif;
      color: #f8f9fa;
    }

    .navbar {
      background-color: #101820;
    }

    .navbar-brand {
      font-weight: bold;
      font-size: 1.5rem;
    }

    .container {
      margin-top: 50px;
    }

    .weather-card {
      background-color: #1b2735;
      border-radius: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      padding: 30px;
      max-width: 600px;
      margin: 0 auto;
      text-align: center;
    }

    .weather-card h2 {
      font-weight: bold;
      margin-bottom: 20px;
    }

    .temperature {
      font-size: 3rem;
      font-weight: bold;
      margin: 20px 0;
    }

    .weather-description {
      font-size: 1.2rem;
      margin-bottom: 20px;
      text-transform: capitalize;
    }

    .weather-icon {
      margin-bottom: 20px;
    }

    .search-form {
      margin-top: 20px;
    }

    .search-input {
      border-radius: 25px;
      border: none;
      padding: 10px 20px;
    }

    .search-btn {
      background-image: linear-gradient(45deg, #ff477e, #ff6f3c);
      border: none;
      border-radius: 25px;
      color: #fff;
      padding: 10px 20px;
      font-weight: bold;
      margin-top: 10px;
    }

    .error-message {
      background-color: #ff6f3c;
      color: #fff;
      border-radius: 10px;
      padding: 10px;
      margin-top: 20px;
    }
  </style>
</head>

<body>

  <div class="container">
    <%= yield %>
  </div>

  <!-- Incluir Bootstrap JS desde CDN -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.min.js"></script>
</body>
</html>
